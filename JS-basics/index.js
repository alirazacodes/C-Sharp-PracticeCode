// //console.log('Hello world');

// let a = 'red';
// let b = 'blue';

// let c = a;
// a = b;
// b = c;

// console.log(a);
// console.log(b);


// for-in loop: to itereate over the properties of an object wihtout or with iterables.
// for-of loop: similar to the previous one but uses of instead of 'in' and is used to iterate over the elements of the array,
//              hence it works with iterables only.
// Note: Objects are not iterable.

const numbers = [1, 2, 3, 5];

console.log(numbers.indexOf(6));

